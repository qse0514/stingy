# Stingy Backlog（W12 D4 整理 · 全量 bug + 需求清单）

> 规矩：**只列不动工**，学员挑了哪个再做哪个。bug 细节全在 `docs/review-week12-day4.md`（修复工单，交新会话）。
> 状态标记：🔴 bug ｜ 🟡 改进 ｜ 🟢 新功能 ｜ 📝 先记录不修

## 已完成（W12 D4，供对照）

- ✅ F-A 预算与超支提醒（budgets 表 + set_budget/query_budget + 回执捎带提醒；exp:budget 14/14，全量回归绿）
- ✅ S1 月度仪表盘（GET /api/stats/summary + 侧栏入口 + 四卡面板；浏览器真机验收，截图存档）
- ✅ 综合评审工单（6 bug + 6 改进 + architecture.md 校对）

## M1 对账与自检可靠性（bug 密集区，建议整包给修复会话）

| # | 级 | 内容 | 大小 |
|---|---|---|---|
| B1 | 🔴 | hasRecentConfirmed 用提案**创建**时间判"最近确认"→ 确认发生在创建 10 分钟后时，自检把诚实草稿纠成谎话且零告警 | 中 |
| B2 | 🔴 | 预算超限回执"已拒绝执行"以"已"开头 → 被规则⑥当成功 | 小 |
| B3 | 🔴 | 一笔成功+一笔失败混合结局，诚实回复被规则②误报 | 小 |
| — | 📝 | LLM-as-judge + Eval 测试集（学员已拍板不做，挂账） | 大 |

## M2 前端聊天健壮性

| # | 级 | 内容 | 大小 |
|---|---|---|---|
| B4 | 🔴 | sendMessage 不查 res.ok：后端 4xx/5xx 时空气泡干等 | 小 |
| B5 | 🔴 | 30 秒总超时压不住多轮 Agent Loop → 前端报超时、后端照常落库 → 用户重发有重复记账风险。改空闲超时 | 小 |
| I1 | 🟡 | 确认卡片 409 不读 reason，可能把"已过期"标成"已确认" | 小 |
| I2 | 🟡 | appendMessage(user) 在 try 外，DB 抖动时错误穿 SSE 衣服出门 | 小 |
| I6 | 🟡 | 窄屏响应式：侧栏 w-64 固定宽挤爆主区（S1++ 评审窄窗截图实锤；学员拍板量级到了再修） | 中 |
| U3 | 🟢 | Markdown 渲染（模型回复现在裸文本直出；需加依赖 react-markdown） | 小 |

## M3 HITL 提案中心（已知欠账转正）

- 🟢 卡片刷新即丢 → GET /api/pending + 侧栏"待确认"入口带角标 + 列表页确认/拒绝（顺手解决 I1）｜中

## M4 会话管理

- 🟢 会话删除 → **已并入 S1++ 工单** `ticket-week12-day4-stats-ui-2.md` T7（硬删+事务，拍板已定）；重命名仍挂账｜中
- 📝 I5：messages 表无限增长，归档策略 ｜大

## M5 数据与统计扩展

| # | 级 | 内容 | 大小 |
|---|---|---|---|
| B6 | 🔴 | deleted:true + group_by 汇总文案不说"未计入统计" | 小 |
| S2 | 🟢 | 账目明细表格页：筛分类/时间、显示软删状态 —— 对账机制的 UI 化 | 中 |
| S1+ | ✅ | 仪表盘增强：月份切换 / 流水列表 / 饼图替横条 / 改名「报表」/ 柱图底线 bug —— W12 D4 已落地验收（见 devlog 交付表） | 中 |
| S1++ | 🟢 | 报表页视觉优化：两列栅格 / 概览卡合并（日均+预测）/ 分段导航 / 圆心总额 / 空月收敛 + 会话删除 —— 已拍板，工单 `ticket-week12-day4-stats-ui-2.md` 交新会话 | 中 |
| S3 | 🟢 | 聊天内嵌图表（[STATS] 第四暗号，工具结果结构化下发） | 大 |

## M6 预算完善（F-A 延伸）

- 🟢 取消预算（现在 set_budget 只有 upsert，设了删不掉）｜小
- 🟢 仪表盘里直接编辑预算（不用回聊天说话）｜中

## M7 观测层

- 🟢 Trace 回放页（案卷有数据没界面）｜大
- 📝 I3/I4：hasRecentConfirmed 不分会话、syncMemoryReceipt LIKE 全表扫（量级到了再修）

## 建议排法（老师意见，学员拍板）

1. M1+M2 的 6 个 bug → 新修复会话（工单 `review-week12-day4.md` 已备好）
2. 本会话下一个模块：M3 提案中心 或 S2 表格页（中等大小、看得见摸得着）
