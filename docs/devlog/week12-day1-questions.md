# Week 12 Day 1 · 问题与踩坑记录

> 主题：Memory 中期（对话归后端管 / buildHistory 重建历史）；下午追加：HITL 确认回写模型记忆（trace 34623773 事故定位与根治）
> 背景：接 W11 D5 欠账 —— Memory 顺延到本周。副产品：`docs/architecture.md`（项目结构与实现总览，以后改码前先查它）

## 提问记录

| # | 问题 | 一句话答案 |
|---|------|-----------|
| 1 | `buildHistory` 里那段 `list.map(...)` 详细讲讲 | 它是**持久化层 ↔ API 合同的翻译器**：库里是一张扁平表（5 列，不用的填 NULL），API 要三种形状（回执 / 申请表 / 普通对话）。map 按两个线索还原：`role==='tool'` → 回执（`tool_call_id` 是认领申请表的凭据）；`assistant && tool_calls` → 申请表（TEXT 原文此刻才 `JSON.parse`，存时原样存 = 保真；`content` 故意不兜底，API 允许 null）；兜底 → 普通消息。⭐ 分支顺序有讲究：兜底若放前面，申请表的 `tool_calls` 会被丢掉，产生孤儿回执，下次请求 API 直接 400。`?? ''` 只是让类型过关，真实保证在写入端 |
| 2 | 现在的实现是不是有个根本性缺陷？（贴出 trace 34623773 谎报 + trace eb570701 重复提交的日志） | 是。**HITL 确认那条路径完全绕过模型的记忆**：用户点确认 → `handleConfirm` 只改 `pending_expenses` 和 `expenses`，`messages` 表里模型的记忆停在"待确认…尚未记入"。下一轮 `buildHistory` 重建出来还是旧状态 → 模型要么断言"没记上"（反向谎报，规则⑤抓到的就是它），要么重新提交一遍（全靠 10 分钟幂等窗口兜底 —— 超过 10 分钟就是真的重复记账）。三层机制（记忆/HITL/对账）各自都对，**接缝处漏了**：状态变了，但记忆没人通知 |

## 踩坑记录

| # | 现象 | 原因 | 处理 |
|---|------|------|------|
| 1 | 用户确认完提案 #28，问"记好了吗"，模型答"还没记上"（trace 34623773，会话 #8 真实数据） | confirm 路径不经过模型也不写 messages，模型记忆里最后一句仍是"尚未记入" | `pending.ts` 新增 `syncMemoryReceipt`：confirm/reject 时①整句重写那张"待确认"回执 + ②会话末尾追加成对【申请表+新回执】 |
| 2 | reflect 明明拦下了草稿，最终回复还是同一句谎话（trace 34623773 里 reflect 和 mismatch 是同一条 reason） | 零工具路径的纠正话术说"如实告诉用户你还没有执行"——对规则③（吹牛）方向对，对规则⑤（反向谎报）**方向反了**：纠正指令自己把模型推回谎话里 | `audit.ts`：Verdict 加 `recordedFact`，规则⑤触发时 reflect 换一套"以已落库事实为准"的话术 |
| 3 | 只重写旧回执还不够：实验第二轮模型又说"还没记上呢" | 模型自己那句"还没记上，请确认"的旧正文排在回执**后面**，就近锚定赢了重写 —— 真相必须同时是【可信通道（工具回执）】和【最新的一句话】 | `syncMemoryReceipt` 追加成对的申请表+回执到会话末尾（孤儿 tool 行会让 API 400，所以必须成对） |
| 4 | 修好反向谎报后，诚实的"记好了"反而被规则③报警 | 规则③不查真库：零工具 + CLAIM_ADD 一律定罪，可确认后的下一轮凭（已回写的）记忆说"记好了"是合法的 | 规则③先 `hasRecentConfirmed()` 再定罪（对称于规则⑤）。已知代价：确认后 10 分钟内凭空吹的也放过，出路是 D4 的 LLM-as-judge |

## 验证

- `npm run exp:confirmsync`（新脚本，走真实记忆层复现事故现场）：2/2 通过 —— 回执重写 ✅、"记好了吗"答"已入账 #N" ✅、对账一致 ✅、零新增提案 ✅。第二次跑还看到完整防线：模型仍先起草了谎话 → reflect 用新话术拦下 → 终稿说真话。
- `npm run exp:afterconfirm`（旧幂等脚本）回归通过：零新增。
- `npx tsc --noEmit` 前后端均 0 错误。

## 今天的一课

**每一条状态变更路径都要问一遍：谁的记忆需要知道这件事？** confirm 是全系统唯一一条不经过 runAgent 的状态变更路径，所以它也是唯一一条忘了给记忆发通知的路径——不是哪一层写错了，是层与层的接缝没人负责。

---

# 主会话补录（全天：②⑥ 收尾 + ③ Memory/多会话 + UI 改版 + Planning 理论）

## 提问补录

| # | 问题 | 一句话答案 |
|---|------|-----------|
| 3 | lets wrap up 2 and 6 first, but **what is 4??** | ④ = Reflection 只覆盖 no_tool_call 那条出口。Agent Loop 有两种走完方式：模型主动收尾（出口 A，有草稿可审）/ 跑满 5 轮（出口 B，draft=''，reflect 第一行直接放弃）。⭐ 撞满轮数的请求恰恰最乱，自检在最需要它的路上不工作。🔴 今天没修，挂在欠账里 |
| 4 | you see the problem?（贴乱恢复 #31 的日志+截图） | ⭐⭐ 四环事故链：①回执活不过请求边界，它手上没有 #38（Memory 的洞）→②自检拦了但纠正无效（零工具路上没回执可"以其为准"）→③想查也查不到（查账把已删的过滤了，恢复没有眼睛）→④猜了 #31，幂等回执"已恢复…本来就没删"把模型和对账双双骗过。模型自己诊断对了："我删的时候没保留编号" —— 又一次"模型是对的，工具错了" |
| 5 | 删除不走 HITL（指出老师的理由有问题） | ⭐ 学员对："软删可逆所以不用 HITL"是循环论证 —— 可逆靠人进库敲 SQL，而"只能进库改"正是要修的病。结论不变（D5 三条真理由在删除上一条不成立），但要三件事配套：回执复述删了什么 + restore_expense 工具 + 对账新规则。"可逆必须是产品里的动作，不是数据库里的一种可能" |
| 6 | explain the whole implementation and flow of memory + multi convo | 所有权一句话：前端从"历史持有者"降级成"显示器+遥控器"，只寄 conversationId+新消息；真身在 messages 表（含工具回执）。同一堆行两副眼镜：buildHistory 给模型（全要），listDisplayMessages 给人（只要正文） |
| 7 | history 具体调了哪些函数？为什么要独立的 sql 文件，不能每次敲命令？ | 记忆层共 5 个函数（create/exists/append/buildHistory/listDisplay）；写入口只有 appendMessage 一个。schema.sql：**库是"现在的状态"，文件是"可以再来一遍的能力"** —— 换机器/建测试库/部署全靠它；SELECT 证明现在，schema.sql 保证将来。项目没 git，它还是表结构唯一的重建来源 |
| 8 | i dont think we need a planning function for something simple like stingy? | ⭐⭐ **学员对，系列第二问**（D5 第一问是 multi-agent）。四个判据（步数>3且有依赖/错路代价高/人需要预审意图/需要预算）Stingy 全不沾，F1 车队经理全沾。而且给 Stingy 加计划确认 = 把 HITL 关卡从信息最完整处搬回最早处，是倒退。**教学清单上的能力 ≠ 这个产品需要的功能** —— Planning 的代码写在 F1 D3（回放页消费它那天） |

## 踩坑补录（主会话，含老师自己的）

| # | 现象 | 原因 | 处理 |
|---|------|------|------|
| 5 | query_expenses 明细回执里根本没有 #id（SELECT 查了、格式化时丢了） | 改/删只能按编号动手，没编号整条链是断的；tsc 全绿查不出 | 行首加 #id + 抬头说明用途 |
| 6 | 对账回归断言第一版假通过（"已经帮你改好了"一个词都没命中 CLAIM_ADD） | 跟 D5 判分正则没验证同一个错 | ⭐ 方法论：**每条补丁配一个对照组** —— 同一句话在空案卷下必须被判谎报，证明它真有杀伤力 |
| 7 | restore 对没删过的记录回"已恢复…本来就没被删除" → 乱恢复 #31 事故 | ⭐ 幂等边界搬错：(1,0) 在 delete 里是"删过又删"=真幂等，在 restore 里是"根本没删过"=打错目标；"已"开头还把对账协议骗了 | 回执改"未恢复（#id）：当前不处于删除状态"。⭐ 只有前一次操作真发生过，第二次才叫重复；一列状态记不下历史，只能说"当前不处于" |
| 8 | exp:memory 断言 D2 第一次跑就挂（排序平手） | DATETIME 秒级精度，bump 和填充消息同一秒 → id DESC 兼并让错方赢；⭐ 老师十行前刚在注释里写过这个坑，立刻踩进去 | 确定性拨时间（NOW()-INTERVAL），不靠 sleep |
| 9 | 下午规则③补丁后 exp:crud 挂 23/24（下午那位 AI 没跑这个回归） | 规则③多了 hasRecentConfirmed 前置查询 = 对账规则第一次依赖真库当下状态，断言⑯被自造的 confirmed 提案安抚；断言⑫-对照当时没炸只是窗口碰巧已过 | 实验先清自造提案，断言按窗口分两支。⭐ 新规矩（已进 architecture.md 不变量 9）：**给规则加环境依赖，测这条规则的实验也在爆炸半径里** |
| 10 | 老师写中文注释又坏字 5 次（阈→阀、兑→兜、拦→拓、嘞→嚸、另有两处） | 老毛病，手册早有记录；本行第一版自己又把坏字对写坏了一次 | 全部回读时抓到改回；写完必复查的规矩继续有效 |
| 11 | 删除不走 HITL 的第一版论证是循环论证（学员指出） | 拿"可逆"当理由，而可逆的兑现方式正是要修的病（进库改） | 补齐三件套：回执复述 + restore 工具 + 查已删参数（恢复的眼睛） |

## 交付表（主会话）

| 块 | 内容 | 验证 |
|---|------|------|
| ⑥ traces 上限 | startTrace 每 20 次开卷清一次，只留 200 卷，整卷删 | exp:prune 5/5（备份→验备份→清场→测→还原） |
| ② 改/删/恢复 | update/delete/restore 三工具 + 软删 deleted_at + 防绕过 HITL（大额改金额被挡回）+ 对账规则⑥及③⑤补丁 | exp:crud 24/24 |
| ③ Memory 中期 | conversations/messages 两表、buildHistory（30条+user对齐防腰斩）、新合同 {conversationId,message}、transcript 落库（catch 也落） | exp:memory 11/11 |
| 多会话 | 侧栏列表/新建/切换，首次发送才建会话 | 学员浏览器实测 |
| 事故修复 | restore 第三结局"未恢复" + query 加 deleted 参数 + 零工具专属纠正话术 | exp:crud ⑮⑯ + 学员浏览器复测（trace 25a32907 链路正确） |
| UI | ChatGPT 式改版（深色侧栏/无气泡 AI 消息/圆角输入条）+ Markdown 裸奔修复 | 学员实测 + tsc/build 0 |

## 遗留欠账

| 欠账 | 严重度 | 去向 |
|---|---|---|
| 🔴 F1 骨架一行未动（D1 硬指标） | 高 | D2 第一件事 |
| 🔴 Reflection 出口 B（跑满 5 轮无草稿，自检关闭） | 中 | 学员未拍板修，D2 顺手（~20min） |
| Planning 代码 | 中 | ⭐ 不在 Stingy 写（学员论证成立），F1 D3 回放页那天写 |
| HITL 提案列表页 / 确认卡片刷新丢失 | 低 | W12 重做 |
| 规则③ 10 分钟窗口内吹牛放过 | 低（显式取舍） | D4 LLM-as-judge |

