# 复制这个文件为 .env，填入你自己的 key（.env 已被 gitignore，不会被提交）
DEEPSEEK_API_KEY=sk-xxxxxxxx
PORT=3001

# 🗄️ MySQL（Week 11 Tool Calling：记账用）
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=stingy
