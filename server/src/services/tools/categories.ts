// 🗄️ 合法分类白名单：菜单的 enum 和后端校验共用这一份
//    （前端校验是礼貌，后端校验是命 —— 这里是"命"那一份）
//    ⚠️ 单独一个文件的理由：addExpense 和 queryExpenses 都要用它，
//       放在任一个工具文件里，另一个 import 过去就成了工具之间互相依赖
export const CATEGORIES = ['餐饮', '交通', '购物', '娱乐', '居家', '医疗', '其他'];
